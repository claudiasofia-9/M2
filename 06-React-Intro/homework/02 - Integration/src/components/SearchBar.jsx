export default function SearchBar(props) {
   return (
      <div>
         <input type='search' />
         {/* <button onClick={}>Agregar</button> */}
         <button>Agregar</button>
      </div>
   );
}
